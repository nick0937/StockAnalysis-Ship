# 台股每日個股觀察報告 — 執行守則（合併版）

> 本檔由 `報告守則.md`（規範）與 `排程prompt_本機版.MD`（可執行 prompt）合併而成，**一份文件同時扮演兩個角色**：可當規範查閱，也可整份餵給排程直接執行。
> **★ 本檔為唯一權威來源。** Cowork skill `tw-stock-8`（`/tw-stock-8`）內含的是 2026-08-11 舊版，已不再同步；若兩者衝突，**一律以本檔為準**。
> **★ 本檔設計為可完整復刻的規格書**：另一個 session 只要讀本檔＋複製 `報告樣式.css`，即可在別的 repo 產出格式完全相同、只換標的的報告。移植步驟見**第 16 節**。
> 最後更新：2026-08-12｜合併時衝突處一律採 2026-08-11 版守則（詳見第 17 節）
>
> **同組檔案**：`報告守則.md`（本檔・規格）＋`報告樣式.css`（版型）＋**`tools/`（參數化產生器，附 2026-08-11 完整實測資料可直接跑）**＋`建立Commit.bat`（建立 commit）
>
> **★ 不要從零重寫產生器。** `tools/` 已把整條流程參數化：改 `tools/config.py` 的標的清單與基準日、更新 `tools/inputs/*.py` 的當日資料，跑五支程式即可。詳見 [`tools/README.md`](tools/README.md)。本檔是規格與判讀依據，`tools/` 是執行工具，兩者互補。
> 適用標的：StockAnalysis-Ship 資料夾的 10 檔航運族群追蹤組合

---

## 0. 這份報告是什麼

每個交易日收盤後，對 10 檔追蹤標的產出一份 **mobile-first RWD 的單一自足 HTML**，內容涵蓋大盤環境、技術面、籌碼面、財報三表、基本面、新聞事件，最後給出**雙情境操作建議**，並存進 `StockAnalysis-Ship` 資料夾（GitHub Pages）。全程用繁體中文。使用者是 Nick。

**執行摘要（給排程用的一句話）**：
> 請產出「台股航運族群每日個股觀察報告」HTML 儀表板，交付給使用者並存到 StockAnalysis-Ship 資料夾。依本檔第 3 節確認可執行後，依序執行第 4～13 節。

---

## 1. 追蹤標的（10 檔，航運族群）

> **★ 移植時只需改這一節與第 16.3 節列出的參數。** 其餘章節（資料來源、指標公式、評分權重、版面規格、存檔順序、檢查清單）與追蹤標的無關，可原樣沿用。

| 代號 | 名稱 | Yahoo 代碼 | 市場 | 產業 |
|---|---|---|---|---|
| 2603 | 長榮 | 2603.TW | 上市 | 航運（貨櫃海運／遠洋航線） |
| 2609 | 陽明 | 2609.TW | 上市 | 航運（貨櫃海運／遠洋航線） |
| 2615 | 萬海 | 2615.TW | 上市 | 航運（貨櫃海運／亞洲近洋線） |
| 2610 | 華航 | 2610.TW | 上市 | 航空（客運／貨運） |
| 2618 | 長榮航 | 2618.TW | 上市 | 航空（客運／貨運） |
| 2646 | 星宇航空 | 2646.TW | 上市 | 航空（客運） |
| 6757 | 台灣虎航 | 6757.TW | 上市 | 航空（低成本航空 LCC） |
| 2645 | 長榮航太 | 2645.TW | 上市 | 航空維修（MRO／發動機與機體大修） |
| 2637 | 慧洋-KY | 2637.TW | 上市 | 航運（散裝海運／長期租船） |
| 2606 | 裕民 | 2606.TW | 上市 | 航運（散裝海運） |

🆕 **2026-08-17 全數換為航運族群（原 9 檔電子／化工／機械組合全部移除）。** 10 檔皆為證交所上市，Yahoo 代碼一律 `.TW`；`6757`／`2645`／`2646` 雖是新掛牌股，實測 `.TWO` 一律 404、exchange 回 `TAI`，**不要因為 6757 是 6 開頭就寫成 `.TWO`**。

⚠️ **首期報告的三件必辦查證**（沿用第 16.3 節，缺一項就會出錯）：
1. **面額變更／減資／增資換股** → 查到要填 `config.SPLIT`，否則近 8 季 EPS 年增率會失真 N 倍。
2. **除息日、停券期間、法說會、財報公告期限** → 填 `config.TIME_PRESSURE`。
3. **`tools/inputs/` 九個檔案的 key 全部要換成這 10 檔**（籌碼、月營收、財報三表、季度推算、題材新聞、評分、買賣區間、概述、大盤與建議書文字）。目前仍是舊組合 2026-08-11 的範例資料，缺任何一檔的任何一項，`build_report.py` 會直接 KeyError 中止。大戶門檻 `CONC_THRESHOLD` 必須逐檔讀頁面表頭，不可假設 400 張。

📌 **航運族群特有的判讀變數**（依第 16.3 節第 7 點，加在第 9 節的消息面與基本面說明裡，**五面向權重不動**）：
- 貨櫃（2603／2609／2615）：SCFI／CCFI 運價指數週變化、美東美西線報價、新船交付與拆船量、聯盟艙位調度、旺淡季。
- 散裝（2637／2606）：BDI 及 BCI／BPI／BSI 分項、鐵礦煤穀物貨載、長租船合約覆蓋率與租約到期結構。
- 航空（2610／2618／2646／6757）：客座率與載客人次、貨運載運率與航空貨運費率、油價與匯率（燃油成本、美元負債）、機隊交機進度與航點恢復。
- 航空維修（2645）：機隊在修量能、發動機大修訂單與客戶集中度。
- 全族群共同：油價（燃油成本）、新台幣匯率、關稅與美中貿易政策、地緣政治（紅海／巴拿馬運河繞航推升運價）。

> 註：本組合（航運 10 檔，repo `StockAnalysis-Ship`）與另外兩套不同，不要搞混——`StockAnalysis` 是電子／化工／機械 9 檔，`StockAnalysis_MySelf` 是持股報告（00935／1504／1514／2327／2603／2610／2618）。三者的 `2603`／`2610`／`2618` 會重複出現，但評分與建議各自獨立產出。

---

## 2. ★ 資料取得原則（優先於後續各步驟的來源清單）

1. **一律以瀏覽器（Claude in Chrome）為主要取得方式**。沙箱對 Yahoo、玩股網通常被擋（回 000），不要反覆試 curl 或 web_fetch；第 0 步測完網路若不通就直接走瀏覽器，不用猶豫。
2. **來源少而精，不為湊來源數而多開站**。每類資料一個主來源、最多一個備援：
   - 行情與技術指標 → Yahoo Finance chart API（在瀏覽器 page context 用 `fetch()` 同源抓）
   - 財報三表 → Yahoo 奇摩股市財務報表頁（20 季、免登入）
   - 籌碼（法人／資券／主力／大戶）→ 玩股網 WantGoo
   - 大盤與櫃買當日收盤 → 玩股網 `all-quote-info`
   - 月營收、重訊、新聞 → 鉅亨網（新聞搜尋 API 好用）；補充才用經濟日報／工商時報
3. **台股數字準確度高，不需過度交叉驗證**。收盤價做一次 Yahoo vs 玩股網比對即可；財報只在「解析可能出錯」時才驗證（例如逗號切分歧義，用 `FCF = CFO + CFI` 這類恆等式檢查）。不要為每個數字再開第三個站。
4. **子代理只查新聞、題材、目標價**這類非結構化資訊，不要派子代理重抓已有主來源的行情、籌碼、財報（它們拿到的來源反而更雜、更慢）。每檔新聞 3~5 則即可。
5. **唯一例外：數字看起來不合理時必須查清楚**（EPS 驟降九成、股價腰斬、毛利率單季掉 20 個百分點以上），多半是面額變更、減資、除權息或財報認列方式改變，這種時候該查幾個來源就查幾個。
6. **★ 瀏覽器用完一定要關閉分頁**。這是使用者的電腦，不要留一堆分頁佔資源。
   - 每一「類」資料抓完就關（行情關、籌碼關、財報關），不要等到最後才一次收拾；中途若被中斷，殘留的分頁不會有人幫忙關。
   - 用 `mcp__claude-in-chrome__tabs_close_mcp` 逐一關閉；全部完成後呼叫 `tabs_context_mcp`（`createIfEmpty: false`）確認回傳「No tab group exists」。
   - 整份報告盡量**重用同一個分頁**（用 navigate 換頁），不要每抓一個網站就開新分頁。
   - Chrome 若中途斷線又恢復可能產生新的 tab group，收尾要再確認一次。

**工具載入**：走瀏覽器路線前，用一次 ToolSearch 把需要的工具一起載入：
```
select:mcp__claude-in-chrome__tabs_context_mcp,mcp__claude-in-chrome__navigate,mcp__claude-in-chrome__javascript_tool,mcp__claude-in-chrome__browser_batch,mcp__claude-in-chrome__tabs_close_mcp
```

---

## 3. 執行時機

### ★ 3.0 先判斷要跑哪一種：日報 vs 即時（關鍵字對照）

本 repo 有**兩條完全不同的流程**，接到指令的第一件事就是分辨要跑哪一條，**不要靠猜**。

| 要跑的東西 | 觸發關鍵字（命中任一即可） | 實際動作 | 產出 |
|---|---|---|---|
| **日報**（收盤後完整） | 「日報」「今日報告」「收盤報告」「完整報告」「跑報告」「跑台股報告」「今日個股觀察」「昨天的再跑一次」、`/tw-stock-8` | 第 4～13 節全流程 | `YYYYMMDD/index.html`（每日一份、永久保留） |
| **即時**（盤中要不要動手） | 「**即時**」「盤中」「live」「即時建議」「即時頁」「現在能不能買」「現在該不該賣」 | **只跑 `python tools/build_live.py`**，不抓籌碼、不更新任何 `inputs/`、不重算指標 | `live/index.html`（單一份、每次整份覆蓋） |

判定規則：

1. **先看關鍵字**。命中「即時」欄任一字眼 → 走即時流程；否則 → 走日報流程。「跑報告」不帶即時字眼一律當日報。
2. **兩邊都沒明講、而當下是盤中（09:00~13:30）** → **先問一句**「要跑即時頁，還是等收盤跑完整日報？」再動手。此時最容易誤跑成整套日報而抓到未完成 K 棒。
3. **哪一組標的由當前工作目錄決定**：`StockAnalysis-Ship` = 本檔 10 檔航運組合；`StockAnalysis` = 電子／化工／機械 9 檔組合；`StockAnalysis_MySelf` = Nick 持股組合。使用者明確講「持股」「我的持股」「自選」時才切換，且應提醒他換資料夾。
4. **即時流程的前置條件**：`tools/data/indicators.json` 與 `inputs/` 必須是最近一個完整收盤日的日報跑完後留下的。若基準日已落後（例如昨天沒跑日報），即時頁的區間與評分是舊的，**必須在回覆與頁面上明講基準日**，或改建議先補跑日報。
5. 即時流程**不建 commit、不改首頁**（首頁入口按鈕是 `finalize.py` 產的，`build_live.py` 只就地更新時間戳）。

細節與鐵則見**第 12A 節**。

### 3.1 日報的執行時機

- 本報告設計在**收盤後（13:30 之後）**執行。
- **週六、週日或台股休市**：只回「今日台股未開盤，略過報告」即結束，不要產出任何檔案。
- **盤中啟動**（09:00~13:30）：Yahoo 會回傳未完成的 K 棒（成交量 0、最低價等於開盤價），必須依「只保留日期 ≤ 最近一個完整收盤日」濾除，並以前一交易日為基準日，同時在報告與回覆中講清楚。
- **資料時差**：融資融券約當日 21:00 才公布；大戶籌碼為週資料。若在 17:30~18:30 產出，資券會落後一個交易日，須在表下標註；隔日補跑可拿到完整資料，**籌碼判讀可能因此明顯不同**（曾發生「大戶連續下滑」補齊後變成「回升」、以及 5371 建議由「觀察偏多」下修為「觀察」的反轉）。

---

## 4. 第 0 步：環境自檢

> 本任務在**使用者自己的電腦（Windows 11 + PowerShell）**上執行。以下同時列出 Windows 本機與 Linux 沙箱兩套指令，依實際環境擇一。

### 4.1 取台北時間，判斷是否交易日與是否已收盤

- Windows：`Get-Date -Format 'yyyy-MM-dd HH:mm:ss ddd'`（本機時區已是 Asia/Taipei；若不確定用
  `[System.TimeZoneInfo]::ConvertTimeBySystemTimeZoneId([DateTime]::UtcNow,'Taipei Standard Time')`）
- 沙箱：`TZ=Asia/Taipei date`

### 4.2 確認資料夾路徑（記為 `$SA`）

- Windows：`$SA = 'C:\Users\user\Desktop\StockAnalysis-Ship'`（已知固定路徑，直接用）
- 沙箱：`find / -maxdepth 6 -type d -name StockAnalysis-Ship 2>/dev/null`

確認 `$SA` 內有 `.git`、`index.html`、以及若干 8 位數字資料夾。

### 4.3 測對外網路

- Windows：`curl.exe -s -o NUL -w "%{http_code}" "https://query1.finance.yahoo.com/v8/finance/chart/2330.TW?range=5d&interval=1d"`
- 沙箱：`curl -s -o /dev/null -w '%{http_code}' 'https://query1.finance.yahoo.com/v8/finance/chart/2330.TW?range=5d&interval=1d'`

回 **200** → 走 **路線 A（直接抓）**；回 **403／000／逾時** → 走 **路線 B（Claude in Chrome）**。實務上幾乎都是路線 B。

### 4.4 開工前先講一句

「路徑 = ?、網路 = 路線 A/B、收盤狀態 = ?」，後面出問題才知道卡在哪。

### 4.5 兩條路線都失敗時

**不要硬產報告。** 寫一份簡短說明（今天抓不到行情、卡在哪一步、錯誤訊息），用 SendUserFile 交付後結束。**寧可不出報告，也不要出假數字。**

---

## 5. 第 1 步：行情與技術指標

抓 `^TWII`、`^TWOII` 與 10 檔標的的
`https://query1.finance.yahoo.com/v8/finance/chart/{SYM}?range=2y&interval=1d`
存下後**在本地計算所有指標**（不要用第三方計算值）。

- **路線 A**：寫一支 Python 腳本帶正常 User-Agent header 直接抓，存成 JSON。10 檔一次跑完，不需分批。
- **路線 B**：navigate 到 `https://query1.finance.yahoo.com/v8/finance/chart/%5ETWII?range=1y&interval=1d`，之後在該分頁 page context 用 `fetch()` 同源抓其餘標的，**分批列印（每次 3~4 檔）**避免截斷。

用 `regularMarketTime` 確認收盤時間。濾掉 `close` 為 null 的列，只保留日期 ≤ 最近完整收盤日。

**指標清單（大盤與個股同一套）**：收盤、漲跌、漲跌%、開高低、成交量（張＝volume/1000）、MA5/10/20/60/120/240、20/60 日乖離率、KD(9,3,3)（含前一日）、RSI(14)（含前一日）、MACD 的 DIF/DEA/柱狀體（含前一日與前二日）、布林(20,2) 含 %B 與帶寬、5/20 日均量、量比、52 週高低與距高點%、20/60/120/240 日報酬率、今年以來報酬、近 20 日收盤序列與日期。

**櫃買指數**：Yahoo `^TWOII` 歷史序列長期停更（目前停在 2026/07/17）。務必檢查最後一筆日期；若不等於今日，只取當日收盤值（玩股網 `https://www.wantgoo.com/investrue/all-quote-info` 中 `id === 'TWO'` 的 `close`／`previousClose`；`id === '0000'` 為加權指數），並在報告明確註明「櫃買歷史序列不可用，大盤技術指標與所有個股相對強弱一律以加權指數為基準」。

**加權指數成交量**：Yahoo 當日常回 0，量比因此標「查無」，改用玩股網的成交股數與成交金額（含前一日對照）判讀量能。

### 大盤環境分（0–100，全體共用）

六項逐項標記正面(+)或負面(−)，並在報告列出組成：

| 項目 | 判斷依據 |
|---|---|
| 均線位置 | 站上幾條（6 條均線） |
| MACD 柱狀體 | 方向與擴縮、DIF 是否趨近零軸 |
| 中期趨勢 | 年線之上與否、YTD |
| 量能 | 成交金額與股數對前一日、是否站上 8,000 億 |
| KD 位階 | K > 80 偏高、> 85 進入鈍化區 |
| 近 20 日報酬 | 正負 |

**對照**：六條均線全站上且量增 → 75~85；均線多頭但量縮或 KD 偏高 → 58~68；均線糾結 → 45~55；跌破月季線 → 25~40。

### 個股相對強弱

對每檔算對**加權指數**的超額報酬（個股報酬 − 指數報酬）1／5／20／60 日，以及 60 日 Beta 與相關係數。

```
RS 分 = 50 + clamp(ex5×1.0 + ex20×1.2 + ex60×0.6, −35, +35)，再 clamp 到 15~85
大盤面分 = 大盤環境分 × 50% + RS 分 × 50%（四捨五入取整，用 floor(x+0.5) 半進位）
```

⚠️ **高 RS 但相關係數低**要判斷是「基本面獨立轉強」還是「題材脫鉤」，後者要在卡片明講風險，不要讓高分掩蓋本業狀況。

---

## 6. 第 2 步：籌碼面（玩股網 WantGoo）

每檔 5 頁。**★ 取用期間（2026-08-12 定案）**：法人動態、資券變化、主力進出一律取 **近 10 個交易日**；大戶籌碼取 **近 13 週（約 3 個月）**；月營收取 **近 12 個月**。

| 資料 | URL（`https://www.wantgoo.com` 起） | 取幾列 | 欄位 |
|---|---|---|---|
| 法人動態 | `/stock/{code}/institutional-investors/trend` | **10** | 日期｜外資(不含自營)｜外資自營｜投信｜自營自行｜自營避險｜總合｜估計持股外資(張)｜投信(張)｜自營(張)｜三大法人合計(張)｜外資持股比重%｜三大法人持股比重% |
| 資券變化 | `/stock/{code}/margin-trading/synopsis` | **10** | 日期｜資餘｜資增｜券餘｜券增｜券資比%｜資券互抵｜當沖率%｜收盤價｜漲跌%｜成交量 |
| 主力進出 | `/stock/{code}/major-investors/main-trend` | **10** | 日期｜收盤價｜主力買賣超｜家數差｜5日集中度%｜20日集中度% |
| 大戶籌碼 | `/stock/{code}/major-investors/concentration` | **13**（週資料） | 年度/日期｜**{門檻}張以上%**｜外資持股%｜投信持股%｜自營商持股%｜董監持股% |
| 月營收 | `/stock/{code}/financial-statements/monthly-revenue` | **12**（月資料） | 見下方 JSON 端點 |

### ★ 大戶籌碼的門檻必須逐檔擷取表頭，不可假設

門檻**每檔不同**（本組合中 8070 長華* 與 8422 可寧衛* 是「1000 張以上」，其餘 6 檔是「400 張以上」）。
- **一律讀該頁表格第 2 欄的表頭文字**，把實際門檻連同數值一起記錄，並寫進報告的表頭與註解。
- **來源網站的選定規則不明**（曾誤寫為「依股本自動選定」，那是臆測）。報告只能寫「門檻由來源網站決定、會隨個股條件而異，以該檔頁面實際顯示者為準」，**不要推定規則**。
- 報告必須加一句提醒：**跨個股比較此欄位前先確認門檻是否相同**（8070 的 69.18%／1000 張與 6548 的 87.63%／400 張不可直接比大小）。
- 玩股網為二手資料，底層應為集保戶股權分散表（TDCC）；若要驗證可查 `https://www.tdcc.com.tw/portal/zh/smWeb/qryStock`，但非必要。

### 抓法

資料為非同步載入，固定 wait 不可靠，要用**輪詢式 JS**。通用刮取器（同時處理有／無 `thead` 的表格，並用關鍵字挑正確的表）：

```js
const KW='券資比';            // 法人='外資'／資券='券資比'／大戶='董監'／主力='家數差'
const N=10;                    // 大戶用 13
const g=()=>[...document.querySelectorAll('table')].map(t=>{
  const hd=[...new Set([...t.querySelectorAll('tr')].slice(0,3)
    .map(r=>[...r.children].map(c=>c.innerText.trim().replace(/\s+/g,'')).join('|')))].join(' ## ');
  const rows=[...t.querySelectorAll('tbody tr')]
    .map(r=>[...r.children].map(c=>c.innerText.trim().replace(/\s+/g,'').replace(/,/g,'')).join('|'))
    .filter(s=>/^\d{2,4}[\/-]\d{1,2}/.test(s));
  return {hd,rows};}).filter(x=>x.rows.length&&x.hd.includes(KW));
let a=[];for(let i=0;i<25;i++){a=g();if(a.length)break;await new Promise(x=>setTimeout(x,500))}
a.length?('LABEL|'+a[0].hd.split(' ## ')[0]+'\n'+a[0].rows.slice(0,N).join('\n')):'NO_TABLE'
```

用 `browser_batch` 把「navigate + javascript_tool」成對串起來，**一次跑 1~2 檔**避免 timeout。
⚠ **單次 javascript_tool 不可超過 45 秒**（CDP 逾時），所以輪詢上限用 `25 次 × 500ms`，不要寫成 50 次 × 700ms。
抓完關分頁。股權分散頁需登入，不抓。

### 月營收（JSON 端點，比刮 DOM 乾淨）

在該檔的月營收頁 page context 內 `fetch()`：

```js
const r=await fetch('/stock/{code}/financial-statements/monthly-revenue-data',
                    {headers:{'X-Requested-With':'XMLHttpRequest'}});
const j=await r.json();
// 每筆：date(ms) / monthRevenue(仟元) / preYearMonthRevenueDiff(YoY%)
//       preMonthRevenueDiff(MoM%) / monthTotalRevenue(累計) / preTotalRevenueDiff(累計YoY%)
j.slice(0,12).map(x=>[new Date(x.date+28800000).toISOString().slice(0,7),
  x.monthRevenue,x.preYearMonthRevenueDiff,x.preMonthRevenueDiff,x.preTotalRevenueDiff].join(','))
```

`date` 是該月 1 日的台北午夜（UTC 前一日 16:00），取年月要先 `+8h`。

### ★ 玩股網 JSON 端點的兩個硬限制

其他四類也有 `-data` JSON 端點（例如 `/stock/{code}/institutional-investors/trend-data?topdays=20`），但：
1. **`topdays` 只吃 `20`**，填 60、5、3 一律回 400 BadRequest。
2. **會驗 Referer 且必須與該子頁面完全吻合** —— 在資券頁打法人端點回 400、跨代號也回 400。
   ⇒ 除了月營收（在自己的頁面打、且各頁都會載入）之外，**其餘四類直接刮 DOM 比較省事**。
3. 本機 curl 重放同樣回 400（Cloudflare session），**必須在瀏覽器內執行**。

**注意事項**
- 資券變化頁有多張表，要挑**表頭含「券資比」**的那張（另一張是「融資券使用率」欄位不同）。
- 主力進出頁的表頭是「**5日集中**」不是「5日集中度」，且**不在 `thead` 裡**，要從前 3 個 `tr` 取表頭、關鍵字用「家數差」。
- **★★ 資券未公布就整段略過，不要放落後一日的表**（2026-08-12 定案）。融資融券約當日 21:00 才公布，若在盤後不久產出，資券最新列會是前一交易日。
  - **做法**：程式自動比對「資券最新列日期」與「基準日」，不一致就**整個「資券變化」區塊不渲染**，不要顯示落後一日的表格——那會讓讀者誤把前一日數字當成當日資料。是否公布由程式判斷，不要用手動旗標（會忘記改）。
  - **仍要抓、仍要用於評分**：融資 10 日增減與券資比變化以最新可得值納入籌碼面 30% 的判讀，並在各檔的建議段落直接引述關鍵數字（例如「券資比 14.18%」「融券最後回補日 08/11」）。
  - **要在兩處說明**：(1) 標題區的指引句後面加一行，說明本期略過與原因；(2) 建議書的「資料完整性說明」列一條，寫明「有抓、用於評分、但不顯示」以及隔日補跑即可完整呈現。
  - 隔日補跑時資券會補齊，此時區塊自動恢復顯示，**籌碼判讀可能因此明顯不同**。
- 若在盤中抓，要濾掉當日未完成列。
- 大戶籌碼為週資料，同一週內重跑不會更新。
- 除息／停券期間資券欄位會出現「--」（融券強制回補），要標「查無」並查明原因（通常是除息或股東會停止過戶）。

### 抓完必做的驗證

- **法人五欄買賣超相加 = 「總合」欄**：外資(不含自營) + 外資自營 + 投信 + 自營自行 + 自營避險 = 總合。逐檔逐日驗，N 檔 × 10 日全過才算對。
- **收盤價交叉驗證**：主力進出表或資券表的收盤價 vs Yahoo 計算值，逐檔比一次。

### ★ 依 10 日／13 週資料判讀（不要只看最近 3~4 日）

短窗口會把「一次性軋空」誤判成「持續買盤」，也看不到大戶的中期流向。評分時至少要算出：

| 統計 | 為什麼重要 |
|---|---|
| 法人 10 日合計、**買超天數**、近 4 日 vs 前 6 日 | 分辨「持續買」與「單日暴買」。曾發生某檔 10 日買超 +11,241 張中有 +11,374 張全集中在最近 4 日、前 6 日反為淨賣 −133 張 |
| 外資持股比重 10 日變化（百分點） | 比單日買賣超更能看出實際部位方向 |
| 主力 10 日合計、家數差 10 日合計 | 家數差合計為負＝籌碼持續集中 |
| 5 日集中度「現在 vs 5 日前」 | 只看當日看不出集中度正在升或降 |
| 融資 10 日合計增減、券資比首末變化 | 券資比由 4.63% 暴增到 14.18% 這種變化，單日看不出來 |
| 大戶門檻欄位「最新 vs 4 週前 vs 13 週前」 | 3 個月才看得到中期進出。曾揭露某檔投信持股由 7.16% 降到 2.58% 幾近出清 |

---

## 7. 第 3 步：財報三表（全檔都做，★ 抓取但**不顯示**）

> **★★ 2026-08-12 定案：財報三表照樣完整抓取並用於評分，但個股卡片上不再顯示三張表。**
> 卡片上的財報只呈現兩張：**月營收（近 12 個月）** 與 **每股盈餘（近 8 季）**。
>
> - **仍要抓、仍要算**：三表數據餵給基本面 20% 內部的「三率與 EPS 趨勢 30%」「財務結構 20%」「獲利品質 20%」。沒有它們就評不出基本面分數。
> - **不顯示的補償措施**：關鍵數字必須被「建議｜兩種情境」與「操作參考」的敘述**直接引述出來**，例如「近四季 CFO÷淨利 0.12」「三率同步走弱（毛利 53.05%→48.21%）」「負債比 58.45% 為 N 檔最高」。讀者不看表也要能掌握重點。
> - **建議書的「資料完整性說明」開頭必須有一段**，明列哪些資料「有抓、用於評分、但不顯示」（三表、公司題材、新聞、超額報酬／Beta／相關係數），避免被誤認為沒做。

**單一主來源：Yahoo 奇摩股市財務報表頁**（20 季、免登入）
`https://tw.stock.yahoo.com/quote/{代號}.TW(或.TWO)/income-statement`、`/balance-sheet`、`/cash-flow-statement`、`/eps`

> 財報狗免費版只給 4 季（8 季需登入，不做）；MOPS 官網對程式抓取會擋。上述四頁已足夠，不需再開其他站。

**表頭必須標明「最新已公布季別」與資料來源**；查不到的欄位標「查無」，**嚴禁自行估算或留白**。

### 抓法（2026/08 實測可用）

**⚠ 這個網域不能用 Claude in Chrome。** `tw.stock.yahoo.com` 在 Claude in Chrome 是**政策封鎖**（navigate 直接回「Navigation to this domain is not allowed」，不是詢問許可），只能改用內建瀏覽器 `mcp__Claude_Browser__`。該瀏覽器**不支援 top-level await**，JS 要包成 `(async()=>{ ... })()`（它會自動 await 回傳的 Promise）。

**★ 用同源 iframe 一次抓多檔多頁，不要逐頁 navigate。** N 檔 × 4 頁若逐頁導覽要 4N 次來回；改成在已開啟的 Yahoo 頁面內建 iframe 載入其餘頁面、讀 `iframe.contentDocument`（同源可讀），一次 JS 呼叫可抓 2~3 檔 × 4 頁：

```js
(async()=>{
 function load(path){return new Promise(res=>{
  const f=document.createElement('iframe');
  f.style.cssText='width:1280px;height:900px;position:fixed;left:-4000px;top:0';
  f.src=path; document.body.appendChild(f);
  let n=0; const t=setInterval(()=>{n++;
   let d=null; try{d=f.contentDocument}catch(e){}
   const ok=d&&d.body&&/年度\/(月份|季度|季別)|單季每股盈餘/.test(d.body.innerText);
   if(ok||n>40){clearInterval(t);
     const r=d&&d.body?{txt:d.body.innerText,doc:d}:null;
     setTimeout(()=>{try{f.remove()}catch(e){}},50); res(r);}
  },500);});}
 // for (const sym of [...]) for (const page of [...]) await load('/quote/'+sym+'/'+page)
})()
```

⚠ 單次呼叫別超過 45 秒的 CDP 逾時 —— 一次抓 2~3 檔（8~12 個 iframe）為上限。

- **損益表／資產負債表／現金流量表**：用 `document.body.innerText` 逐行切，找**最後一個**符合 `/^年度\/(月份|季度|季別)$/` 的錨點，之後取季別與各科目名稱後方的數字。科目名稱：`營業收入`／`營業毛利`／`營業利益`／`稅後淨利`／`總資產`／`總負債`／`股東權益（淨值）`／`流動資產`／`流動負債`／`營業現金流`／`投資現金流`／`融資現金流`／`自由現金流`。
  ⚠️ **不要用 `h2/h3` + `closest('section')` 定位**，導覽列有同名標題會抓錯。
- **EPS 頁相反**：要用 `h2` 找「單季每股盈餘」再 `closest('section')` 限定範圍，否則會被側邊欄「網友也在看」的股價汙染。該區塊每列為「季別｜EPS｜季增率%｜年增率%｜季均價」。
- ⚠️ **逗號切分陷阱**：Yahoo 數字含千分位逗號，用逗號串接輸出會產生歧義（`2,558,220,431,819` 可能是 4 個數也可能是 2 個數）。輸出改用 `|` 分隔，或在本地用「剛好 N 個合法數字＋各數字位數最一致」的約束解析還原，並用恆等式 **自由現金流 = 營業現金流 + 投資現金流** 逐季驗證（Yahoo 的 FCF 就是這個定義，實測 8 檔 × 6 季全數成立）。驗證做一次就夠。
- Yahoo 未提供存貨、應收帳款與資本支出，因此**速動比與 CapEx 一律標「查無」**，不要為補這兩欄再翻 MOPS。

### 三張表的內容

**(1) 損益表 — 近 8 季**

| 欄位 | 單位 |
|---|---|
| 季別 | 例 2026Q2 |
| 營收 | 億元 |
| 營收 YoY% | |
| 毛利率% | |
| 營益率% | |
| 淨利率% | |
| 單季 EPS | 元 |
| EPS YoY% | |

判讀重點：**三率（毛利率／營益率／淨利率）方向**是同步走升、同步走弱，還是「增收不增利」（營收年增但毛利率／EPS 年減）。三率同步走弱要在該檔卡片明確示警。

**(2) 資產負債表 — 最新季 / 前一季 / 去年同季**
總資產、總負債、股東權益、流動資產、流動負債、**負債比%、流動比%、每股淨值**（速動比、存貨、應收帳款：Yahoo 未提供，標「查無」）
判讀重點：負債比方向、流動比變化、每股淨值與股價淨值比。

**(3) 現金流量表 — 最新季 + 近四季合計**
CFO、CFI、CFF、自由現金流（= CFO + CFI，Yahoo 定義）、稅後淨利、**CFO ÷ 稅後淨利**（CapEx 查無）
判讀重點：**CFO ÷ 淨利 < 1 代表獲利沒轉成現金，必須示警**；FCF 為負要說明是擴產投資還是本業失血。
⚠️ 注意 FCF 為正不代表本業好——可能是投資活動流入（處分投資）撐起來的，必須同時看 CFO。

### 兩個必須處理的陷阱

- ⚠️ **季報公布時程**：Q1 約 5/15、Q2 約 8/14、Q3 約 11/14、年報約 3/31 前。**同一組 10 檔的「最新已公布季」經常不一致**（8 月上旬跑報告時部分公司可能仍是 Q1，Q2 在 8/12~8/14 密集公布），務必逐檔標明季別，不同季的數字不可混在同一列。若基準日落在財報公布週，要在「必須注意的事件」列出各檔財報日。公司已公告 H1 但 Yahoo 三表尚未更新時，用「H1 減 Q1」推算單季並**明確標示為推算值**，其單季現金流標「查無」。
- ⚠️ **面額變更股**：8422 可寧衛*（2025/11，面額 10→1 元、1 股拆 10 股）與 4763 材料*-KY（2025/06/27 換發基準日，同為 1:10）。跨越基準日的 EPS 必須統一除以 10 換算到現行股本基礎並在表下註明換算方式，**Yahoo 頁面顯示的 EPS 年增率未做此調整因而嚴重失真**（曾出現 -94.71% vs 正確的 -47.1%、-94.50% vs 正確的 -44.96%）。換算後可用「近四季 EPS 合計是否等於公司公告年度 EPS」驗證。

---

## 8. 第 4 步：基本面、目標價、新聞

用 general-purpose 子代理平行處理（4 個子代理，每個 2 檔），蒐集：

- 公司題材
- 最新月營收（月份、金額、YoY、MoM、累計 YoY）
- 最新財報摘要（EPS、毛利率、營益率、近四季 EPS、H1 EPS、股利殖利率）
- 近三個月具名券商目標價與評等
- 近 30 天 3~5 則新聞（日期、標題、來源、URL）
- 風險提示 2~4 點

**要求子代理 15 分鐘內回報。**

- **目標價查不到就寫「近期查無公開具名券商目標價」，嚴禁杜撰**；TradingView／FactSet 彙總可列但必須標明「彙總非單一券商」。
- **當日有大漲大跌一定要查證原因**；若市場傳言查無任何公開新聞佐證，必須明講「查無公開新聞佐證」，且**不得計入正面消息分數**。
- 來源以鉅亨網為主（新聞搜尋 API `https://ess.api.cnyes.com/ess/api/v1/news/keyword?q=關鍵字&limit=20`，重訊也會轉載全文），需補充才用經濟日報 money.udn.com 或工商時報 ctee.com.tw。**不要為了湊來源把 Yahoo、財報狗、histock、中時全部翻一遍**——行情、籌碼、財報三類已有主來源，子代理只負責新聞、題材、目標價。

---

## 9. 第 5 步：五面向評分

**權重：籌碼面 30%、技術面 20%、基本面 20%、大盤面 20%、消息面 10%**（財報數據**併入基本面 20%**，不另設面向、不調整權重）

| 面向 | 評估內容 |
|---|---|
| 籌碼面 30% | 三大法人近 4 日買賣超方向與連續性、外資持股比重變化、主力買賣超、家數差（負＝集中為佳）、5/20 日集中度、融資增減（減少為佳）、券資比、大戶 400 張以上趨勢 |
| 技術面 20% | **判讀分**（★ 先依 9.0 錨定表選區間、再於區間內判讀）：均線排列與站上／跌破、KD 位置與交叉、RSI、MACD 柱狀體翻紅／翻綠、乖離率、量價配合、距 52 週高低、%B ／ **客觀加減分**：見下方 9.1，由程式計算後疊加 |
| 基本面 20% | 內部配權見下 |
| 大盤面 20% | 依第 5 節公式計算，**不要主觀給分** |
| 消息面 10% | 新聞正負面、題材落地程度（實際出貨 vs 純想像）、法人目標價與現價差距、重大事件（財報日、除息、停牌、標案、法說會） |

**基本面 20% 的內部配權**：月營收動能 20%（YoY／累計 YoY）、**三率與 EPS 趨勢 30%**（近 8 季損益表）、財務結構 20%（負債比、流動比）、**獲利品質 20%**（CFO÷淨利、FCF）、評價 10%（本益比、股價淨值比、殖利率）

### 9.0 技術判讀分的錨定表（2026-08-20 新增，先錨定、再判讀）

判讀分評的是**當日的絕對技術狀態**，不是「比昨天多扣幾分」。每天評分時先依站上均線條數選錨定列，再用 KD、RSI、MACD、乖離、量價、距 52 週高低在區間內定位（比照 §5 大盤環境分的對照表做法）：

| 客觀狀態 | 錨定區間 |
|---|---|
| 站上 6/6 條均線且量價配合 | 72~85 |
| 站上 4~5/6，多頭結構未破 | 58~70 |
| 站上 3/6 上下，均線糾結、KD／RSI 中性 | 45~57 |
| 跌破月線或季線，KD 下彎 | 30~44 |
| 站上 0~1/6 且爆量下跌或創波段新低 | <30 |

**三條鐵則**：

- 判讀分原則上落在錨定區間內；要超出區間 ±5，必須在 `scores.py` 註解寫明理由（例如爆量長黑、跳空缺口、單日 K 值劇變）。
- **同一事實不得逐日重複扣分**。「持續收在 5 日線下」在跌破當天扣過之後，只要狀態沒有進一步惡化，分數就不該再往下調——變動摘要照樣寫 delta，但分數必須先過錨定表。2026-08-19 實測教訓（StockAnalysis 9 檔組）：8 檔仍站上月線、8 檔 MACD 柱在零軸上、RSI 全落 44~56 中性區，判讀分平均卻被逐日下修到 43.8（落入 <45 的減碼帶），就是差分制棘輪下漂的結果。反向也一樣：本 repo 08/19 有 6 檔站上 6/6 全多頭，判讀分卻只評 64~76，是同一種錨定失準。
- **大盤同步下跌的部分交給大盤面 20% 反映**（環境分已依 §5 下修會扣到每一檔），技術判讀分只評個股自身的絕對狀態，不要把「大家都在跌」再扣一次。

**機械錨定防呆（`lib.tech_anchor`）**：`build_report.py` 建置時會用 `indicators.json` 的站上均線條數算出上表的錨定區間（另用 KD／MACD／RSI／量價／距 52 週高／r20 給一個區間內參考落點），手評判讀分超出區間 ±`TECH_ANCHOR_TOL`（5 分）就在終端印警告提醒複查；**只警告、不覆寫、不進報告**，最終分數仍以人工判讀為準。⚠ 不要改成 0~100 的合成分再比偏離——實測合成分在強多頭族群（本 repo 08/19 有 6 檔）會頂到 95~100，與上表上緣 85 的尺度打架，10 檔會誤報 7 檔。

### 9.1 技術面的客觀加減分（2026-08-19 新增，`lib.tech_adj`）

技術面分 ＝ `scores.py` 的**判讀分** ＋ **客觀加減分**（±10 封頂，再夾在 0~100）。
加減分由 `build_report.py` 自動套用並在卡片上完整列出明細，**手填無效**。

| 項目 | 給分 | 說明 |
|---|---|---|
| MACD 背離（頂／底） | ∓6 | `calc_indicators.macd_div` 偵測，頂背離扣分、底背離加分 |
| MACD 隱性背離 | ∓3 | 隱性頂扣分、隱性底加分 |
| 背離時效 | ≤10 根全權、≤20 根半權、>20 根不計 | 轉折確認後訊號會鈍化但不會瞬間失效，故用分級衰減而非硬性截斷 |
| DMA 當日交叉 | ±2／組 | 3-6、6-12、5-20 三組，黃金交叉加分、死亡交叉扣分 |

**★ 嚴禁重複計分（這是本節最重要的一條）**

- 均線排列、KD、RSI、乖離率、布林、量價配合**已在判讀分內**，一律不得再進入加減分。
- **DMA 只計「當日交叉」這個離散事件，不計零軸與 AMA 的位置**。位置等同於均線多頭／空頭排列，
  計了就是把趨勢算兩次。2026-08-19 первый版實測正是踩到這個坑：位置項讓當日最過熱的 2615 萬海
  反而 +3、總分由 79 升到 80 躍居第一，方向完全相反，故移除位置項。
- 頂／底背離同時出現時**自然相加抵銷**，代表訊號互相衝突、不給方向，這是預期行為不是 bug。

**參數集中在 `lib.py`**：`DIV_ADJ`／`DIV_FULL_BARS`／`DIV_HALF_BARS`／`DMA_CROSS_ADJ`／`TECH_ADJ_CAP`，
調參只改這五個常數，不要散落在 `build_report.py`。

**建議對照（分數落點，僅為參考標籤）**：≥70 買進｜55–69 觀察偏多｜45–54 觀察｜<45 減碼／賣出

每檔要寫：判斷理由（3~5 句，引用具體數字，**且至少一項來自財報三表**，例如「CFO÷淨利 1.32 顯示獲利品質佳」「負債比由 58.2% 升至 61.4%」，不要只寫營收與 EPS）、風險提示、一段**大盤面說明**（解讀超額報酬、Beta、相關係數代表什麼）。

⚠️ **若分數與基本面事實明顯衝突**（分數落在觀察區但公司仍虧損、營收衰退、CFO÷淨利 < 1），要在卡片與總結中明講落差，不要讓分數掩蓋事實。

---

## 10. 第 5A 步：★ 雙情境建議（這才是可操作的結論）

分數只是綜合評比，真正要回答的是兩個不同問題，每檔都要分開判斷、不要混為一談：

### 情境一｜空手時：現在是適合的買入點嗎？
標籤四選一：`可買進`／`分批買進`／`等回檔再買`／`不建議買進`

判斷重點：不只看公司好壞，更要看**現在這個價位的風險報酬比**。即使分數很高，只要 K 值 > 80、20 日乖離率 > 8%、%B > 80 同時出現，或當日已大漲且量比 < 1（量縮上漲），就應該是「等回檔再買」而非「可買進」。必須寫出**具體等待價位**（通常是 5 日線或月線）與**放棄等待的條件**。

### 情境二｜已持有時：應該立即或近期賣出嗎？
標籤四選一：`續抱`／`續抱並設停利`／`逢高減碼`／`近期出場`

判斷重點：持有的機會成本與空手的進場成本不同，**不要因為短線過熱就叫人賣掉基本面好的公司**。要寫出續抱理由（財報／籌碼具體數字）、**停利參考價**、**停損／出場觸發價**，以及是否有時間壓力（停牌、除息、財報日）。

**兩個標籤可以不一致，而且經常不一致** —— 這正是本區塊存在的意義。
例：1717 長興綜合分最高、持有「續抱」，但 K 值 88.87、乖離 +13.00%，空手要「等回檔再買」；6411 晶焱分數在減碼區，但股價淨值比僅 1.57 倍有淨值保護，持有是「逢反彈減碼」而非立刻砍。

### 呈現方式
1. **每檔卡片**新增「建議｜兩種情境」區塊（置於「操作參考（價位）」之前），左右兩欄分別為空手與持有。每欄結構為：標籤 →「操作條件盒」→ 3~6 句理由。手機單欄堆疊、平板以上並排。
2. **★ 操作條件盒（2026/08/12 新增，最重要的可執行資訊）**：標籤只說「該不該」，條件盒說「在什麼價位、滿足什麼條件」。每欄兩列：
   - 空手：`買進區間`（例「63.6 – 68.2 元」）＋下方小字標明錨點來源（例「月線 63.63 ～ 5 日線 68.16」）；`觸發條件`一句話（例「K 值由 88.87 回落至 80 以下，且 20 日乖離率收斂至 +8% 以內」）
   - 持有：`停利區間`／`減碼區間`／`出場區間`（依標籤選用）＋錨點來源；`出場觸發`一句話（例「跌破 5 日線 68.16 元縮手；跌破月線 63.63 元且外資轉連續賣超則全出」）
   - **⚠ 區間端點一律錨定實際技術價位**（均線、布林上下軌、前波關鍵價、除息前收盤），並在小字標明來源，**嚴禁主觀設定價位**。找不到兩個合理端點時，可用「某均線 ～ 其上 N%」但必須寫明。
   - 判定為「不建議買進」者，買進區間寫「**暫不設買點**」，錨點欄寫不進場的原因（例「季線未突破前不進場」），觸發條件則寫**重新評估的門檻**（例「帶量站上季線 76.94 元且 CFO÷淨利由 0.52 改善至 1 以上」）。
   - 有時間壓力者（停牌、除息、財報期限）出場觸發改用**時間觸發**並標粗體（例 5371：「8/21 為最後交易日，務必在此之前完成」）。
3. **總覽排名**的建議欄＝分數標籤（中性灰）＋空手與持有兩個操作條件盒（同上格式）。手機單欄堆疊、平板以上並排。
4. **投資建議書**在「依綜合分數分組」之前，加一張「兩種情境的建議總表」（個股｜收盤｜綜合分｜空手｜**買進區間**｜持有｜**減碼／停利區間**），並在表下說明分數建議與情境建議的差別。
4. **COMMIT_MSG** 除了原本的分數分佈，另加「空手」與「持有」兩行分佈統計，以及逐檔的雙情境一覽。

---

## 11. 第 6 步：產出 HTML（★ 必須是 mobile-first RWD）

用 Python 產生**單一自足 HTML**（CSS 全內嵌、不引用外部資源、不用 localStorage）。

### RWD 硬性規格
- `<meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">`、`html{-webkit-text-size-adjust:100%}`、`body{overflow-wrap:break-word}`
- **Mobile-first**：基準寫手機版，再用 `@media(min-width:640px)` 與 `@media(min-width:1000px)` 往上加。**不要用 max-width 斷點**，所有基準樣式必須寫在 media query 之前
- **絕對不能有橫向溢出**；標題字級用 `clamp()`
- 總覽排名用 CSS Grid（`grid-template-areas`）不要用 table：手機多行、桌機單行
- 資料表格包在 `.twrap{overflow-x:auto;-webkit-overflow-scrolling:touch;overscroll-behavior-x:contain}`，第一欄 `position:sticky;left:0`，手機顯示「← 可左右滑動 →」提示、桌機隱藏
- 指標格：手機 2 欄、平板 3 欄、桌機 4 欄
- 卡片標頭用 `grid-template-areas` 重排
- 頂部置頂快速導覽列（大盤／總覽／各個股／建議書），每張卡片底部「↑ 回頂端」
- 可點元素最小觸控高度 44px；sparkline 用 `preserveAspectRatio="none"` + `vector-effect="non-scaling-stroke"`
- hover 包在 `@media(hover:hover)`；加 `@media print`

### 配色規範
**基本**：紅＝上漲／成長／改善，綠＝下跌／衰退／惡化。**中性數值不要套漲跌配色**（負債比、流動比、每股淨值、Beta、相關係數、RS 分、收盤價、持股比重都是中性數值）。

**★ 建議標籤：顏色深淺代表「該採取行動的急迫性」，不是好壞**
（改版原因：先前分數落點、空手建議、持有建議全套同一組藍階，8 列 × 3 個標籤全是深淺藍，掃過去分不出輕重。）
不需要動作的越淡、越該立刻處理的越深。

| 軸 | 標籤 | 樣式 | 符號 |
|---|---|---|---|
| 空手 | 可買進 | 實心深藍 `#17497f` 白字 | ● |
| 空手 | 分批買進 | 中藍 `#3d7ab8` 白字 | ◎ |
| 空手 | 等回檔再買 | 白底藍框 `#9dbcdd` 藍字（等待，視覺輕） | ○ |
| 空手 | 不建議買進 | 灰底 `#eef1f5` 灰字 `#8b95a3`（視覺重量最低、幾乎隱形） | ✕ |
| 持有 | 續抱 | 白底灰框 `#ccd4de`（什麼都不用做 → 最輕） | ✓ |
| 持有 | 續抱並設停利 | 白底琥珀框 `#e0c27f`、字 `#8a5a00` | ✓ |
| 持有 | 逢高／逢反彈減碼 | 琥珀底 `#c07a12` 白字 | ▼ |
| 持有 | 近期出場 | 磚紅底 `#9e3414` 白字（最重） | ✕ |

- **每個標籤前加符號**，讓灰階列印與色覺障礙也能分辨。⚠️ `◑` 在部分 CJK 字型缺字會變成怪符號，用 `◎`。
- **分數落點標籤降級**為中性灰框 `#f2f5f9` 底、`#dde3ea` 框，前綴「分數」二字，避免與行動標籤搶視覺。
- **★ 總覽排名不放五面向小分**（2026/08/12 改版）：8 列 × 5 個小分＋細條會把版面塞滿、稀釋決策資訊。五面向小分只保留在**各個股卡片內**的「五面向評分」區塊（含基本面內部拆解），總覽只留：排名｜個股｜收盤｜漲跌%｜綜合分｜建議與操作條件。
- **五面向小分（卡片內）不要上色塊**：數字用一般深色文字，下方加 3px 細條表達強弱（≥70 深藍 `#17497f`／45–69 灰 `#9aa8bb`／<45 淺灰 `#d9dee5`）。
- **操作條件盒配色**：白底、`--line2` 細框、圓角 7px。區間數字用 14~15px 粗體、`tabular-nums`；錨點來源用 10.5px 淺灰小字；觸發條件用 11.5~12.5px 次深色。**區間數字本身是中性數值，不套漲跌配色**。
- 總覽表格上方加**圖例**，說明「顏色深淺代表該採取行動的急迫性，不是好壞」並列出全部 8 種標籤。
- `@media print` 時所有標籤改白底黑字加細框，避免列印時大面積色塊。

### ★ 內容結構（2026-08-12 精簡定案，這一節就是驗收標準）

**設計原則：首頁與大盤只放決策資訊，細節下沉到個股卡片；敘述一律概述、不長篇大論。**

**1. 置頂導覽列**
`<nav class="nav"><ul><li><a href="#market">大盤</a></li>…</ul></nav>` — ⚠ **CSS 是寫在 `.nav ul > li > a` 上的**，若輸出裸 `<a>` 連結會變成直排。四個連結：大盤／總覽／各個股／建議書。

**2. 標題區（精簡）**
只有三樣東西：
- `<h1>` 報告標題
- `<div class="meta">` 四格摘要卡：資料基準日｜產出時間｜追蹤 N 檔｜五面向權重
  ⚠ **`.meta` 本身是 grid 卡片容器**，子元素必須是 `<div><span>標籤</span><b>值</b></div>`。若拿它當純文字 `<p>` 用，文字會被塞進 grid 格子而錯亂。
- `<p class="hint">` 一句指引：詳細內容請點總覽的個股名稱進卡片、資料來源見文末建議書
- **不要**放資料來源清單、不要放長段說明（那些移到建議書）

**3. 大盤趨勢區塊（只留指標）**
- 標題列：指數收盤與漲跌（**不放 sparkline**，`.mkt .cspk{display:none}`）
- 指標格：大盤環境分（含「六項中 N 正 M 負」）｜收盤｜漲跌｜成交金額（含前一日與增減%）｜KD｜RSI｜MACD｜乖離率｜52 週高低｜報酬率（1月/3月/6月/1年）｜今年以來
  **不放**：開高低、成交股數、量比、布林
- 六條均線位置條
- 趨勢判讀：**一段**（約 5 句），末尾用 `<small>` 併入「環境分組成（六項各標 +/−）」與「櫃買指數註記」
- **移除**：環境分組成表（獨立表格）、個股相對強弱表 —— 相對強弱資訊已在各卡片的分數與敘述中

**4. 總覽排名**
欄位：`排名｜個股｜收盤｜漲跌%｜綜合分（下方小字為分數落點）｜空手｜持有`
- **不放五面向小分**（8 列 × 5 個小分會塞滿版面、稀釋決策資訊；小分只留在卡片內）
- 空手／持有各一格 `.o-ev` / `.o-hv`，**只放兩樣**：標籤 chip ＋ 價位區間（`買進 63.6 – 68.2 元`／`停利 73.2 – 76.9 元`）
- **錨點來源與觸發條件不放在總覽**，那些在卡片內
- ⚠ **表頭 `.ovh` 與資料列 `.ovr` 是兩個獨立 grid 容器**，用 `auto` 欄寬會各算各的、必然對不齊 → **一律用固定像素寬度**（640px 起：`34px minmax(84px,1fr) 66px 62px 52px 1.5fr 1.5fr`）
- 上方保留標籤圖例（8 種標籤）與一行分數對照

**5. 每檔卡片（★ 只留這 12 個區塊，順序固定）**

| # | 區塊 | 內容 |
|---|---|---|
| 1 | 股價與技術指標 | sparkline ＋ 指標格（含收盤價交叉驗證欄） |
| 2 | 均線位置 | 六條均線位置條 |
| 3 | 法人動態（近 10 個交易日，單位：張） | 13 欄完整表 |
| 4 | 資券變化（近 10 個交易日） | 11 欄，挑含「券資比」的表。**★ 基準日資券未公布時整段略過不顯示**（見第 6 節），資料仍用於評分 |
| 5 | 主力進出（近 10 個交易日） | 6 欄，家數差以「負＝紅」呈現 |
| 6 | 大戶籌碼（近 13 週，約 3 個月） | 表頭標明實際門檻 |
| 7 | 月營收（近 12 個月） | 年月｜營收(億)｜YoY｜MoM｜累計 YoY，包 `.twrap.tnarrow` |
| 8 | 每股盈餘（近 8 季） | 季別｜單季 EPS｜EPS YoY，包 `.twrap.tnarrow.tn3`；表下註明最新已公布季、Q2 推算值、面額變更調整、近四季 EPS／PE／PB |
| 9 | 法人目標價 | 一段話，查無就明說並交代檢索範圍 |
| 10 | 五面向評分 | 五條分數條＋綜合分＋基本面內部拆解 |
| 11 | 建議｜兩種情境 | 標籤 → 操作條件盒 → 2~3 句概述 |
| 12 | 操作參考（價位） | 2~3 句 |

**已移除、不要再放**：財報三表專區、公司題材、近期新聞、大盤面說明、判斷理由、風險提示。
（資料照抓、照評分，關鍵數字改由第 11、12 區塊的敘述直接引述）

- **雙情境區塊**：左右兩欄（`.acts` grid，手機單欄堆疊、平板以上並排）。每欄＝標籤 chip →「操作條件盒 `.opbox`」（買賣區間＋錨點來源＋觸發條件，見第 10 節）→ 2~3 句概述。標頭提問用可操作句式：空手「在什麼價位、滿足什麼條件才買？」、持有「在什麼價位減碼、跌破什麼就出場？」
- **欄位少的表格**（月營收 5 欄、EPS 3 欄）要加 `.tnarrow`，否則會撐滿整個寬度很難看；同時把非首欄的 `th` 靠右對齊，才會跟 `td.num` 對齊。

**6. 投資建議書總結**
一句話總結（結合大盤環境）→ **兩種情境的建議總表**（個股｜收盤｜綜合分｜空手｜買進區間｜持有｜減碼／停利區間）→ 依綜合分數分組 → 當日關鍵變化 → 必須注意的事件（各檔財報公布日／法說會／除息／停牌）→ **資料完整性說明**（開頭先講「有抓但不顯示」那一段，再逐項列出來源與查無項目）

**7. 免責聲明**

### ★ 樣式表：直接複製 `報告樣式.css`

版型不要重寫。repo 根目錄的 **`報告樣式.css`**（約 24,100 字元、142 個 class、14 個媒體查詢）是從實際報告匯出的完整樣式，產生報告時**整段內嵌到 `<style>`**（不可用 `<link>` 外連，違反單一自足規格）。
主要 class：`.nav/.hero/.meta/.hint`、`.card/.stk/.chd/.cnm/.cpx/.cspk/.ctag`、`.grid/.mc/.mcl/.mcv`、`.mabh/.mabs/.mab`、`.twrap/.tnarrow/.tn3/.thint/.tnote`、`.ovt/.ovh/.ovr/.o-rk/.o-nm/.o-px/.o-cg/.o-sc/.o-ev/.o-hv/.ol/.ozn`、`.sbs/.sbr/.sbl/.sbt/.sbv`、`.acts/.act/.acth/.actw/.actq/.actp/.opbox/.aol/.aoz/.aoc`、`.chip` 八種標籤、`.band`、`.foot`。

### RWD 驗證（必做）
用 Playwright 在 **360 / 390 / 768 / 1280px** 開檔，檢查 `document.documentElement.scrollWidth <= innerWidth`；有溢出就修到四個寬度都通過再交付。另檢查大盤區塊標題沒有重疊。

- **Windows 本機**：`pip install playwright` → `playwright install chromium`；或直接用系統已安裝的 Chrome（`channel='chrome'`），不需要 `--no-sandbox`／libXdamage 那一套。
- **Linux 沙箱**（2026/08 實測）：新版 playwright 的 chromium 走 `storage.googleapis.com` 會被擋。改用
  ```
  pip install --break-system-packages 'playwright==1.44.0'
  PLAYWRIGHT_DOWNLOAD_HOST=https://cdn.playwright.dev playwright install chromium   # 會裝 chromium-1117
  ```
  若報缺 `libXdamage.so.1`（無 sudo 無法 apt install）：到 `http://archive.ubuntu.com/ubuntu/pool/main/libx/libxdamage/` 抓 deb、`dpkg-deb -x` 解開後設 `LD_LIBRARY_PATH=<解開路徑>/usr/lib/x86_64-linux-gnu`。launch 參數加 `--no-sandbox --disable-gpu`。

⚠️ **Claude in Chrome 擴充套件無法開 `file://`**（回「Can't interact with browser internal pages」），RWD 驗證只能走 Playwright，不要指望用 Chrome MCP 開本機檔案。

---

## 12. 第 7 步：存檔（★ 注意寫入順序）

在 `$SA`（git repo，remote `git@github.com:nick0937/StockAnalysis-Ship.git`，分支 `gh-page`，GitHub Pages）：

1. 寫報告到 `$SA/YYYYMMDD/index.html`（YYYYMMDD ＝ 資料基準日）
2. **重建總覽首頁**：列出所有 8 位數字資料夾，由新到舊排序，產生同樣 mobile-first RWD 的 `$SA/index.html`（卡片式清單、「閱讀最新一期」大按鈕、10 檔標的標籤、資料來源與免責聲明）
3. **最後才寫** `$SA/COMMIT_MSG.txt`：

```
report(YYYYMMDD): 台股個股報告 N 檔

大盤    加權指數 XX,XXX（±X.XX%）環境分 XX
最高分  <名稱>(<代號>)  <分數>  <建議>
最低分  <名稱>(<代號>)  <分數>  <建議>
分佈    買進 x ｜ 觀察偏多 x ｜ 觀察 x ｜ 減碼 x
空手    <各標籤分佈>
持有    <各標籤分佈>
基準日  YYYY-MM-DD（週X收盤）

雙情境建議
        <逐檔：名稱(代號) 分數｜空手 符號 標籤｜持有 符號 標籤>

重點
- （3~6 條當日關鍵變化）
```

4. 寫完後確認三個檔案 mtime 都是剛剛，並列成表格附在回覆中。**三個檔案都到齊才提示可以發布。**
   - Windows：`Get-ChildItem "$SA\index.html","$SA\COMMIT_MSG.txt","$SA\YYYYMMDD\index.html" | Select-Object FullName,LastWriteTime`
   - 沙箱：`ls -l --time-style=full-iso`
   - 先前發生過根目錄 index.html 落地時間晚於使用者 commit，導致推上 GitHub 的是舊檔。

**★ commit 由流程代跑，push 一律不碰（2026-08-28 修訂，Nick 指定）**：
   跑完即時頁之後（見第 12A 節鐵則 8），<b>接著執行 repo 根目錄的「建立Commit.bat」</b>建立 commit。
   - <b>該 bat 只 commit、不會 push</b>（它自己會印「已建立 commit，尚未 push」），所以這一步不會讓任何內容上線。
   - <b>push 仍然完全由使用者決定</b>：要推上 GitHub Pages 得另外跑「發布到GitHub.bat」或 `git push`，<b>絕對不要代跑</b>。
   - 執行方式：`cmd //c "建立Commit.bat" < /dev/null`（Git Bash）。導向 `/dev/null` 是為了讓結尾的 `pause` 不會卡住；
     若沒有已追蹤檔案的變更，bat 會問「仍要建立一筆空 commit 嗎」，此時吃到 EOF 會自動取消（等同回答 N），屬正常。
   - <b>不要改用手打的 `git add` / `git commit`</b>：bat 內含 index.lock 自動清除、作者資訊檢查、`core.autocrlf false` 設定與 `-F COMMIT_MSG.txt`，繞過它就會失去這些保護。
   - commit 訊息一律來自 `COMMIT_MSG.txt`（它在 `.gitignore` 內，只改它不會觸發 commit）。

---

## 12A. 當日即時投資建議頁（`live/`）

日報是「收盤後的完整判斷」，這頁是「盤中要不要動手」。兩者分工必須清楚：

| | 日報 `YYYYMMDD/` | 即時建議 `live/` |
|---|---|---|
| 內容 | 完整評分、籌碼、財報、敘述 | **只有操作動作** |
| 資料 | 全部為基準日收盤值 | **只有價格／漲跌／成交量是即時的** |
| 保存 | 每日一份，永久保留 | **單一資料夾，每次執行整份覆蓋，不留歷史** |
| 產生 | `build_report.py`（一天一次） | `build_live.py`（盤中可重複跑） |

### 鐵則

1. **不重算任何技術指標與評分。** 均線、布林、20 日均量、五面向分數一律沿用 `data/indicators.json` 與 `inputs/` 的基準日值。盤中 K 棒未完成，拿它重算指標必然失真——這正是守則第 5 節要濾除未完成 K 棒的原因。
2. **判斷邏輯固定、不主觀。** 只做「即時價 vs `inputs/zones.py` 的買賣區間、月線、5 日線」的機械比對：
   - 空手：區間內＝可分批買｜高於區間＝等回檔｜跌破區間下緣＝支撐失守先別接｜`暫不設買點`＝不宜買進
   - 持有：高於／進入賣出區間＝執行 → 跌破月線＝加快處理 → 跌破 5 日線＝先縮手 → 其餘依日報結論
3. **★ 必須尊重日報的持有結論。** 日報判「近期出場」或「逢高／逢反彈減碼」者，即使價格還沒到區間，**也不可以輸出「續抱」**——只能寫「反彈至 X 再減碼」或「仍應在期限內分批出場」。這是最容易出的矛盾。
4. **報價來源與延遲要寫在頁面上。** 取價順序固定為 **鉅亨**（`ws.api.cnyes.com`，真實成交價，實測落後約 1 分鐘）→ **臺灣證券交易所 MIS**（`mis.twse.com.tw`，約 10 秒；**大盤指數只能用此源**，個股的成交價欄 `z` 僅在該次快照剛好撮合時才有值，無值時以最佳五檔中價推估）→ **Yahoo Finance**（**實測固定延遲 20 分鐘**，僅作最後備援）。頁面須標示實際來源、報價時戳與落後量（不足 90 秒顯示秒數），並提醒下單前以券商報價複核。
   > 2026-08-18 實測：Yahoo 的 `regularMarketTime` 本身就落後牆鐘 20 分 00 秒 ±5 秒，不是程式快取造成的；改源後落後量降至 16 秒。**不要改回以 Yahoo 為主源。**
5. **報價若仍停在日報基準日**（未開盤或休市），頁面最上方要明說，不可讓讀者誤以為是新一日的行情。
6. 首頁的入口按鈕由 `finalize.py` 產生（`<a class="big lv">`），時間戳由 `build_live.py` 就地更新 `<span class="lvt">`，兩者不可互相覆蓋。
7. RWD 同樣要過四個寬度：`python verify_rwd.py live`。
8. **★★ 日報跑完一定要接著跑一次即時頁（2026-08-26 定案，Nick 指定）。** `finalize.py` 跑完、三個檔案齊備之後，
   一律接著執行 `python build_live.py` → `python verify_rwd.py live`，讓 `live/index.html` 立刻換到當期基準日，
   **不要等到下次盤中才跑**。理由：`live/` 沿用 `data/indicators.json` 與 `inputs/` 的基準日值，日報跑完若沒重跑，
   首頁的即時入口會一直掛著前一期的區間與時間戳，隔天盤中點進去看到的是舊基準。
   - 收盤後跑的即時頁，報價本來就等於當日收盤價（或休市停在基準日），依鐵則 5 頁面會自動標明，這是預期行為、不是錯誤。
   - 這一步算在交付前檢查裡（見第 13 節第 7 項），沒跑不算交付完成；最終回覆要同時給日報與即時頁兩個路徑。
9. **★★ 即時頁跑完後，接著執行「建立Commit.bat」（2026-08-28 定案，Nick 指定）。**
   順序固定為 `build_live.py` → `verify_rwd.py live` → <b>建立Commit.bat</b>，<b>單獨跑即時頁時同樣適用</b>（不是只有日報那條路徑才做）。
   該 bat <b>只 commit、不 push</b>；push 仍由使用者自行決定，<b>不可代跑</b>。細節見第 12 節。

---

## 12B. ★ 只保留最新 10 個開盤日的報告（2026-08-27 定案，Nick 指定）

`YYYYMMDD/` 資料夾**只留最新 10 期**，超出的一律刪除。

**執行時機（不可省略）**：跑**今日／昨日報告之前**先檢查、先刪除，再開始跑報告。
不要等跑完才清，否則首頁與 `COMMIT_MSG` 會先列進即將被刪的期別。

```bash
cd tools
python prune_reports.py --dry-run   # 先看會刪什麼（可省略）
python prune_reports.py             # 實際刪除，接著才跑當期報告
```

- 保留期數寫在 `tools/config.py` 的 **`KEEP_PERIODS = 10`**，要改留幾期只改這裡；設為 `0` 代表不刪除。
- **`finalize.py` 會再自動呼叫一次**（重建首頁之前），所以就算忘了第 0 步也一定會清乾淨，
  且首頁不會列到已刪除的期別。第 0 步仍要跑，因為那才符合「先清再跑」的本意。
- **安全限制（不要拿掉）**：只處理 repo 根目錄下「檔名剛好 8 位數字」的資料夾（`live/`、`tools/` 一律不碰）；
  資料夾內若有 `index.html` 以外的檔案就**不刪、只警告**，改由人工確認。
- **⚠ 這是不可逆的**：被刪的期別會從 GitHub Pages 上消失（推上去之後）。
  刪除只發生在本機檔案系統，**要等使用者跑「建立Commit.bat」並 push 才會反映到線上**；
  在那之前都還能用 `git checkout -- <資料夾>` 救回。
- 即時頁 `live/` 不受此規則影響（它本來就只有一份、每次覆蓋）。

---

## 13. 第 8 步：交付與檢查

用 SendUserFile 交付 HTML（`display: "render"`）。附一句話摘要：資料基準日、大盤環境分與判讀、分數最高與最低的個股、當日最值得注意的一件事。

**交付前檢查（逐項確認才算完成）**
1. 三個檔案（`YYYYMMDD/index.html`、`index.html`、`COMMIT_MSG.txt`）都已寫入、mtime 為剛剛、順序正確
2. RWD 在 360／390／768／1280px 四個寬度全部通過
3. **瀏覽器分頁已全部關閉**（`tabs_context_mcp` 回傳「No tab group exists」）
4. **沒有殘留的舊分數或舊日期**（改過評分或補跑時特別容易漏）。★ **要寫成程式掃描，不要靠肉眼**：把「改動前的分數」列成 dict，對報告 HTML 與 COMMIT_MSG.txt 逐檔搜尋 `名稱 舊分 分` 與 `(代號) 舊分｜` 兩種樣式。實測有效——這次就抓到 COMMIT_MSG 裡殘留「長興 76 分」（已改為 77）。
   ⚠ 檢查腳本本身也會過時：改版後若某個檢查項目找不到（例如仍在找已移除的「財報三表」、或標題結構已改），**先確認是報告有問題還是檢查條件過時**，不要盲目回頭改報告。
5. 五面向小分與綜合分一致（用程式重算驗證，注意四捨五入要用 `floor(x+0.5)` 半進位，不要用 Python 內建 `round` 的銀行家捨入）
5a. **每檔的操作條件盒都有內容**：10 檔 × 2 欄 = 20 格，每格都要有區間（或「暫不設買點」）、錨點來源、觸發條件三項，不可留白；區間端點的數字要能在該檔的指標格或均線位置條中找到對應
6. 查不到的資料都已標「查無」並在資料完整性說明中列出原因
7. **★ 報告資料夾只剩最新 10 期**：跑報告前已執行 `python prune_reports.py`（`finalize.py` 亦會自動再跑一次），首頁期數 = `KEEP_PERIODS` 或更少（見第 12B 節）
8. **★ 即時頁已同步重跑，且已跑「建立Commit.bat」建立 commit（只 commit 不 push）**：`python build_live.py` → `python verify_rwd.py live` 都已執行，`live/index.html` 的基準日等於本期基準日（見第 12A 節鐵則 8）

---

## 14. 品質要求

- 所有數字必須來自實際抓取，**嚴禁憑印象填寫或杜撰**，查不到就標「查無」
- 收盤價做一次 Yahoo 計算值 vs 玩股網表格收盤價的比對即可，不一致才註明；**其餘數字不需逐一找第二來源**，台股公開資訊準確度高，把時間留給判讀而非重複查證
- **財報數字一律標明季別與公布日**，不同季不可混在同一列；單季由累計數相減推算須標「推算」
- 例外：**數字明顯不合理時必須查清楚**（見第 2 節第 5 點）
- 開始前用 TaskCreate 建立任務清單，完成時更新狀態
- 最終回覆保持精簡，不要重述報告全文

### ★ 14.1 敘述長度上限與「文字棘輪」防呆（2026-08-24 定案）

第 11 節的內容結構已規定第 11、12 區塊各為「2~3 句概述」，但 2026-08-17~08-21 五期實測是**逐期單調上漂**：

| 期別 | 空手/檔 | 持有/檔 | 操作參考 | 目標價 | 合計 |
|---|---|---|---|---|---|
| 08-17 | 386 | 291 | 271 | 147 | 1,095 字 |
| 08-21 | 552 | 569 | 376 | 339 | **1,836 字（+68%）** |

句數實測為空手 6.1 句、持有 6.7 句，**是規格的 2~3 倍**。成因與 §9.0 禁止的「差分制棘輪下漂」完全同源
——**拿上一期的文字去加註，而不是照當期狀態重寫**。分數有 §9.0 防棘輪，文字沒有，所以五期都沒被攔下。

**硬上限（`shorts.py` 與 `zones.py` 的檔頭也各自寫了一份）**：

| 欄位 | 上限 |
|---|---|
| `EMPTY_S`／`HOLD_S`／`OPS_S` | 各 **3 句** |
| `TGT_S` | **2~3 句** |
| 每段 `⚠` | 最多 **1 個** |
| 每段 `<b>` | 最多 **3 段**（超過 3 段等於沒有重點） |
| `zones.py` 每個 `buy_cond`／`sell_cond` 的編號條件 | 最多 **3 條** |

**四條寫作紀律**：

1. **照當期狀態重寫，不要拿上一期的文字去加註。**「前期是 X、本期是 Y」只在「這個變化本身就是決策依據」時才寫。
2. **已解除的 ⚠ 必須撤掉**，不可累積。
3. **已成立或已失效的觸發條件必須退場**；成立的寫成「已成立 → 動作」，不再掛著等下一期。
4. **判讀理由寫在 `shorts.py`，條件盒只放「區間 → 錨點 → 條件」**，不要兩邊各寫一遍。

`build_report.py` 會在每次產出時印出各段的句數／⚠／粗體統計，超過上限即印警告（只提醒、不改內容）。

---

## 15. 已知陷阱清單（踩過的坑，別再踩）

| # | 陷阱 | 解法 |
|---|---|---|
| 1 | 沙箱抓不到 Yahoo／玩股網（回 000） | 直接走瀏覽器，不要反覆試 curl |
| 2 | Claude in Chrome 開不了 `file://` | RWD 驗證走 Playwright |
| 3 | Playwright 官方 chromium 下載被沙箱擋 | 用 1.44.0 + `cdn.playwright.dev`，補 libXdamage；Windows 本機無此問題 |
| 4 | Yahoo 財報三表用 `closest('section')` 會抓到導覽列 | 三表用 body innerText + 錨點；EPS 頁反過來要用 section |
| 5 | Yahoo 財報數字含千分位逗號，串接後無法切分 | 用 `\|` 分隔或約束解析 + `FCF = CFO + CFI` 驗證 |
| 6 | 面額變更股的 Yahoo EPS 年增率嚴重失真 | 8422 與 4763 跨基準日的 EPS 除以 10，並用年度 EPS 驗證 |
| 7 | 盤中執行會抓到未完成 K 棒 | 濾除日期 > 最近完整收盤日者 |
| 8 | 融資融券 21:00 才公布 | 標註落後一日；隔日補跑籌碼判讀可能反轉 |
| 9 | 櫃買指數 Yahoo 歷史序列停更 | 只取玩股網當日收盤，相對強弱一律用加權指數 |
| 10 | 加權指數當日成交量 Yahoo 回 0 | 量比標「查無」，改用玩股網成交金額與股數 |
| 11 | `.git/index.lock` 殘留導致 commit 失敗且訊息誤導 | 已在 建立Commit.bat 加自動偵測清除 |
| 12 | `.gitignore`／`README.md` 因 CRLF/LF 差異永遠顯示 modified | 已轉為 LF 並設 `core.autocrlf false` |
| 13 | COMMIT_MSG.txt 在 .gitignore 中，只改它不會觸發 commit | bat 已會列出 .gitignore 並說明 |
| 14 | 改分數後舊數字殘留在敘述中 | 交付前 grep 掃舊分數字串 |
| 15 | Python `round` 的銀行家捨入導致分數不一致 | 統一用 `floor(x+0.5)` |
| 16 | 市場流傳 4763「AI 伺服器／低損耗 CCL」題材 | **完全不存在**，它是菸用二醋酸纖維絲束化工廠、不在 PCB 供應鏈（2026/08/12 再次獨立查證，命中的全是台燿、聯茂、騰輝-KY、金像電等真正 CCL 股） |
| 17 | 玩股網資料端點參數猜不到（一直回 400） | 端點是 `/stock/{code}/{頁面}-data?topdays=20`，**`topdays` 只吃 20 這個值**（60、3 皆回 400）；且會驗 Referer 必須與該子頁面完全吻合，跨代號、跨頁面都不行。要嘛在對應頁面的 page context 用 `fetch()`，要嘛直接刮 DOM |
| 18 | 大戶籌碼表頭找不到「400張以上」 | **門檻各檔不同**，玩股網依股本自動選定（8070 長華*是「1000張以上」）。刮取關鍵字改用「董監」，並把實際門檻一併記錄下來寫進表頭 |
| 19 | 主力進出頁刮不到（NO_TABLE） | 該頁表頭是「5日集中」不是「5日集中度」，且表頭不在 `thead` 裡。通用刮取器要從前 3 個 `tr` 取表頭、關鍵字用「家數差」 |
| 20 | `tw.stock.yahoo.com` 在 Claude in Chrome 被政策封鎖（直接拒絕、不是詢問） | 該網域改用內建瀏覽器 `mcp__Claude_Browser__`。且該瀏覽器**不支援 top-level await**，要包成 `(async()=>{...})()`（會自動 await Promise） |
| 21 | Yahoo 財報頁 10 檔 × 4 頁要 40 次導覽，太慢 | 在已開啟的 Yahoo 頁面內建**同源 iframe** 載入其餘頁面，讀 `iframe.contentDocument.body.innerText`，一次 JS 呼叫可抓 2~3 檔 × 4 頁。注意單次呼叫別超過 45 秒的 CDP 逾時 |
| 22 | 大盤成交金額只能取當日、無歷史可對照 | 改用證交所官方 `https://www.twse.com.tw/rwd/zh/afterTrading/FMTQIK?date=YYYYMMDD&response=json`，有整月歷史且加權指數收盤與 Yahoo 完全一致（沙箱外可直接 curl） |
| 23 | 本機 curl 抓 Yahoo／玩股網回 429／403 | **帶正常 User-Agent 就會回 200**（`curl.exe -A "Mozilla/5.0 ... Chrome/126.0.0.0 Safari/537.36"`）。這台 Windows 機器不需要走瀏覽器也能抓行情與鉅亨新聞，但玩股網的 `-data` 端點仍受 Cloudflare session 限制、必須在瀏覽器內 |
| 24 | 大戶籌碼門檻假設成 400 張 | **每檔都要讀該頁表頭**。曾因只取資料列未取表頭，把 8422 可寧衛*（實為 1000 張以上）誤記為 400 張 |
| 25 | 只看最近 3~4 日籌碼，把一次性軋空當成持續買盤 | 一律取 **10 個交易日**並計算「近 4 日 vs 前 6 日」。曾發生某檔 10 日買超 +11,241 張中有 +11,374 張全集中在最近 4 日、前 6 日反為淨賣 |
| 26 | 大戶只取 3 週，看不出中期流向 | 一律取 **13 週（約 3 個月）**。3 個月視窗才看得到「投信持股由 7.16% 降到 2.58% 幾近出清」這類訊號 |
| 27 | 導覽列變成直排 | `.nav` 的 CSS 寫在 `.nav ul > li > a`，必須輸出 `<nav><ul><li><a>`，不能用裸 `<a>` |
| 28 | 標題區文字被拆成兩欄錯亂 | `.meta` 是 grid 卡片容器，子元素須為 `<div><span>標籤</span><b>值</b></div>`，不能當純文字 `<p>` 用 |
| 29 | 總覽表頭與資料列欄位對不齊 | `.ovh` 與 `.ovr` 是**兩個獨立 grid 容器**，`auto` 欄寬各算各的 → 一律用固定像素寬度 |
| 30 | 欄位少的表格撐滿整頁很難看 | 加 `.tnarrow` 限制寬度，並讓非首欄的 `th` 靠右以對齊 `td.num` |
| 31 | 單次 javascript_tool 逾時（CDP 45 秒） | 輪詢上限用 `25 次 × 500ms`；iframe 批次一次抓 2~3 檔為上限 |
| 32 | 檢查腳本本身過時導致誤判 | 改版後若檢查項找不到，先確認是報告問題還是檢查條件過時；本次即因仍在找已移除的「財報三表」而誤報 |

---

## 16. ★ 移植到其他追蹤組合（例如航運類股）

要在另一個 git repo 復刻同一份格式、只換標的時，照這張清單做。

### 16.1 先決定 repo 架構

**不要用分支。** GitHub Pages 只從單一來源發布，另一個分支的內容永遠不會出現在網站上；而且兩個分支的 `index.html`、`COMMIT_MSG.txt`、`報告守則.md`、bat 檔會永久衝突。分支是給「同一份東西的不同版本」用的，不是給「兩份並存的內容」。
**建議做法**：新開一個 repo（與現有的 `StockAnalysis`、`StockAnalysis_MySelf` 一致）。若希望一個網站列出多組，才改用「同 repo 子目錄」（`{repo}/shipping/YYYYMMDD/`，舊的 `YYYYMMDD/` 原地不動），代價是根目錄 `index.html` 的產生邏輯要改寫。
⚠ 順手統一分支名：`StockAnalysis` 用 `main`，`StockAnalysis_MySelf` 卻 checkout 在 `gh-page`（單數），這種不一致以後會踩到。

### 16.2 要複製過去的檔案

| 檔案 | 說明 |
|---|---|
| `報告守則.md` | 本檔。複製後**只需改第 1 節的標的清單與第 16.3 節列出的參數** |
| `報告樣式.css` | 完整樣式表，整段內嵌到 `<style>`。**不要重寫版型** |
| **`tools/`（整個目錄）** | **參數化產生器。只改 `config.py` 與 `inputs/*.py`，其餘 6 支程式不用動** |
| `建立Commit.bat` | 路徑不用改（用 `%~dp0` 相對於自身位置），只有 title 與畫面文字寫著 repo 名稱，純顯示 |
| `.gitignore`、`index.html` | 沿用（注意 `COMMIT_MSG.txt` 在忽略清單內，只改它不會觸發 commit） |

### 16.2a `tools/` 的組成（詳見 `tools/README.md`）

**只改這兩處**：
- `tools/config.py` —— 標的清單、repo 路徑、基準日、面額變更股、時間壓力事件
- `tools/inputs/*.py` —— 每次跑報告蒐集的資料與判斷（附 2026-08-11 實際資料當範例）

**不用改**：`lib.py`（公式與格式化）、`fetch_quotes.py`、`calc_indicators.py`、`calc_fin.py`、`build_report.py`、`finalize.py`、`verify_rwd.py`

執行順序：
```
python fetch_quotes.py → calc_indicators.py → （填 inputs）→ calc_fin.py
       → build_report.py → verify_rwd.py → finalize.py
```
`finalize.py` 已內建正確的寫入順序（個股報告 → 首頁 → COMMIT_MSG）與全部交付前檢查。

### 16.3 要改的參數（只有這些）

1. **第 1 節的標的清單**：代號、名稱、Yahoo 代碼（`.TW` 上市／`.TWO` 上櫃）、市場、產業。
2. **面額變更／減資股清單**：新組合若有 1 股拆 N 股的個股，必須查出換發基準日並在第 7 節註明「該季（含）之前 EPS 除以 N」。**這件事漏掉會讓 EPS 年增率失真十倍**。
3. **停牌／換股／併購等時間壓力事件**：像 5371 中光電那種最後交易日，要寫進第 1 節的警告與建議書的「必須注意的事件」。
4. **repo 路徑與 remote**（第 4、12 節）。
5. **報告標題與 `<meta name="description">`**（第 11 節）。
6. **首頁的標的標籤列**（根目錄 `index.html` 的 `.tgs`）。
7. **產業特有的判讀重點**：例如航運要看運價指數（BDI／SCFI）、船噸供給、租船合約；這類「該產業才需要的變數」加在第 9 節的消息面或基本面說明裡，**不要動五面向的權重**。

### 16.4 不需要改的（照抄即可）

資料來源與端點（第 2、5、6、7、8 節）、指標計算公式（第 5 節）、大盤環境分與 RS／大盤面分公式（第 5 節）、五面向權重與基本面內部配權（第 9 節）、雙情境標籤與操作條件盒規格（第 10 節）、版面與配色（第 11 節）、存檔順序（第 12 節）、交付前檢查（第 13 節）、陷阱清單（第 15 節）。

### 16.5 復刻順序（給接手的 session）

1. 讀完本檔，確認第 1 節標的清單已換成新組合。
2. 第 0 步環境自檢（第 4 節）。
3. 依第 5~8 節抓資料。**大盤環境分是全組共用的，同一天不同組合應該一致**——若兩個 repo 同日產出而環境分不同，其中一份算錯了。
4. 依第 9、10 節評分與給雙情境建議。
5. 依第 11 節產出 HTML，**樣式直接內嵌 `報告樣式.css`**。
6. 第 12 節存檔（注意順序）、第 13 節逐項檢查。
7. 不要自己 commit／push。

---

## 17. 合併說明：兩份原檔的衝突處與裁決

本檔合併自 `報告守則.md`（2026-08-11）與 `排程prompt_本機版.MD`（較舊）。衝突處一律採較新的守則版：

| 項目 | 排程 prompt 舊寫法 | 本檔採用（守則版） |
|---|---|---|
| 資產負債表欄位 | 含現金、應收帳款、存貨、短期借款、速動比 | 只留總資產／總負債／股東權益／流動資產／流動負債＋負債比、流動比、每股淨值；Yahoo 未提供者一律標「查無」 |
| 自由現金流定義 | `FCF = CFO − CapEx` | `FCF = CFO + CFI`（Yahoo 定義），CapEx 標「查無」 |
| 報酬率區間 | 1月／3月／6月／1年 | 20／60／120／240 日 ＋ YTD |
| 五面向小分呈現 | 分數條用中性藍階色塊 | 不上色塊，數字＋3px 細條 |
| COMMIT_MSG 範本 | 只有分數分佈 | 另加「空手」「持有」分佈與逐檔雙情境一覽 |
| 面額變更股 | 只列 8422 | 8422 與 4763 都要處理 |
| 籌碼 browser_batch 批量 | 一次 1 檔 | 一次 1~2 檔 |
| 輪詢式 JS | 25 次 × 700ms | 30 次 × 600ms |
| 環境指令 | 只有 Linux 沙箱（`TZ=... date`、`find /`、`ls --time-style`） | 併列 Windows PowerShell 與沙箱兩套（第 4、11、12 節） |

---

## 18. 變更紀錄

| 日期 | 變更 |
|---|---|
| 2026-08-28 | **★ 跑完即時頁後改由流程代跑「建立Commit.bat」**（Nick 指定）：第 12A 節新增鐵則 9、第 12 節「不要自己 commit」改寫為「commit 代跑、push 一律不碰」、第 13 節檢查第 8 項併入。該 bat 只 commit 不 push，<b>push 仍完全由使用者決定，不可代跑</b>；執行方式為 `cmd //c "建立Commit.bat" < /dev/null` |
| 2026-08-27 | **★ 新增第 12B 節「只保留最新 10 個開盤日的報告」**（Nick 指定）：跑今日／昨日報告前先跑 `tools/prune_reports.py` 刪除超出者，`finalize.py` 於重建首頁前會自動再呼叫一次；保留期數為 `config.KEEP_PERIODS = 10`。只處理 8 位數字資料夾，內有 `index.html` 以外檔案者不刪只警告。同步加進第 13 節交付前檢查第 7 項與 `tools/README.md` 的每日流程 |
| 2026-08-26 | **★ 日報流程尾端固定加跑即時頁**（Nick 指定）：`finalize.py` 之後一律接 `build_live.py` + `verify_rwd.py live`，避免首頁即時入口停在前一期基準日。寫入第 12A 節鐵則 8、第 13 節檢查第 7 項與 `tools/README.md` 的每日七步 |
| 2026-08-20 | **★ 新增 §9.0 技術判讀分錨定表 ＋ `lib.tech_anchor` 機械錨定防呆（StockAnalysis／MySelf／Ship 三專案同步，`lib.py` 與 `build_report.py` 與 StockAnalysis 版保持一致）**：檢討「技術分普遍偏低」後確認判讀分是差分制（每天只對前一日加減、無絕對基準），連跌期間同一事實被逐日重複扣分造成棘輪下漂；反向在本 repo 也成立——08/19 有 6 檔站上 6/6 全多頭，判讀分卻只評 64~76。修正：①守則加錨定表（依站上均線條數選區間、超出 ±5 須註明理由、同一事實不得逐日重複扣分）；②`build_report.py` 建置時檢查手評分是否落在錨定區間 ±5 內，超出印警告，只提醒不覆寫不進報告（第一版做 0~100 合成分比偏離，在本 repo 的強多頭族群 10 檔誤報 7 檔，已改為區間制）。以三專案 08/19 資料回測，警告命中 4 檔且皆有意義：StockAnalysis 2049（站上 4/6 卻只評 52）、6548（站上 3/6 評 38 略低）；MySelf 1504（評 76 高於區間上緣逾 5）；本 repo 2615（6/6 全多頭卻只評 64）。其餘 18 檔全在容差內 |
| 2026-08-17 | **★★ 本 repo（`StockAnalysis-Ship`）換組合為航運族群 10 檔**：2603 長榮／2609 陽明／2615 萬海／2610 華航／2618 長榮航／2646 星宇航空／6757 台灣虎航／2645 長榮航太／2637 慧洋-KY／2606 裕民，全數上市、Yahoo 代碼一律 `.TW`（已實測 `.TWO` 皆 404）。同步改：第 1 節標的表、`tools/config.py` 的 `STOCKS`／`REPO`／`TITLE`（改為「台股航運族群每日個股觀察報告」）／清空 `SPLIT` 與 `TIME_PRESSURE`、根目錄 `index.html` 的標題與 `.tgs` 標籤列、`SKILL.md`。第 1 節另補「航運族群特有判讀變數」（SCFI／BDI／油價／匯率／機隊與運價），**五面向權重不動**。⚠ `tools/inputs/*.py` 與 `tools/data/*.json` 仍是舊組合資料，首期報告必須整批換掉 |
| 2026-08-13 | **★ 追蹤標的由 8 檔增為 9 檔：新增 2049 上銀**（2049.TW，上市，機械／滾珠螺桿・線性滑軌・工業機器人）。已寫入 `tools/config.py` 的 `STOCKS`；`tools/inputs/` 尚未有其資料，首次納入的那一期必須補齊九個檔案，並查證面額變更與除息／法說會日期 |
| 2026-08-13 | **★ 新增第 3.0 節「日報 vs 即時的關鍵字對照」**：「即時／盤中／live／現在能不能買」只跑 `build_live.py`，其餘一律為完整日報；盤中未講清楚要先問。同步寫入 `tools/README.md` 開頭與 `.claude/skills/tw-stock-8/SKILL.md`（skill 已改為本機薄入口，雲端版停用） |
| 2026-08-13 | **★ 新增「當日即時投資建議」頁（第 12A 節）**：`live/index.html` 單一資料夾、每次執行整份覆蓋，由 `build_live.py` 以 Yahoo 即時報價比對日報的買賣區間，只輸出操作動作；首頁新增獨立入口按鈕；`verify_rwd.py` 可指定 `live`／`home` 目標 |
| 2026-08-13 | 08/12 資券與法人估計持股補齊後重跑：籌碼面 1717 88→86（融資單日 +733 張、券資比 1.47%→4.59%）、5371 42→44（融券回補 408 張、券資比 14.18%→11.66%），5371 綜合 45→46；排名與建議標籤未變 |
| 2026-08-12 | **★ 資券未公布就整段略過**（不再放落後一日的表；資料仍抓取並用於評分，由程式自動判斷）；產生器修掉「推算股數固定取最新一列」的 bug——當日估計持股常未更新，須往下找最新有值者，否則每股淨值與 PB 全變「查無」 |
| 2026-08-12 | **★★ 移植化改版（本檔改為可完整復刻的規格書）**：新增第 16 節「移植到其他追蹤組合」；完整樣式匯出為 `報告樣式.css`（24,100 字元、142 class）供直接複製內嵌 |
| 2026-08-12 | **★ 版面二次精簡**：標題區改為 4 格摘要卡＋一行指引；大盤只留指標格＋均線條＋一段判讀（移除 sparkline、環境分組成表、個股相對強弱表）；總覽建議欄只留標籤＋價位區間；**個股卡片收斂為 12 個固定區塊**（移除財報三表專區、公司題材、近期新聞、大盤面說明、判斷理由、風險提示），敘述改為 2~3 句概述 |
| 2026-08-12 | **★ 財報顯示改制**：三表仍完整抓取並用於評分，但不再顯示；卡片改放「月營收近 12 個月」與「每股盈餘近 8 季」；建議書須說明哪些資料「有抓、用於評分、不顯示」 |
| 2026-08-12 | **★ 籌碼期間定案**：法人／資券／主力取近 10 個交易日，大戶取近 13 週（約 3 個月），月營收取近 12 個月；新增依 10 日／13 週判讀的統計清單（近 4 日 vs 前 6 日、大戶 13 週變化等） |
| 2026-08-12 | 依 10 日／13 週完整資料重新校準籌碼面分數，8 檔中 7 檔變動（4763 84→78、8070 62→55、5371 56→48、8422 80→74、2231 44→38、6411 38→36、6548 54→60）；2231 由「觀察」落入「減碼／賣出」、8070 持有由「續抱」下修為「續抱並設停利」 |
| 2026-08-12 | 修正大戶門檻認定：一律讀頁面表頭、不推定規則（8422 實為 1000 張以上）；新增陷阱 24~32 |
| 2026-08-12 | **★ 版面改版**：總覽排名移除五面向小分（改只留在個股卡內）；建議欄升級為「操作條件盒」＝買賣區間＋錨點來源＋觸發條件，區間端點一律錨定實際技術價位；建議書總表加買進／減碼區間兩欄；交付前檢查新增第 5a 項 |
| 2026-08-12 | 新增陷阱 17~23（玩股網 `topdays=20` 與 Referer 驗證、大戶門檻各檔不同、主力頁表頭差異、Yahoo 財報頁在 Chrome 被封鎖需改內建瀏覽器＋同源 iframe、證交所 FMTQIK、本機 curl 帶 UA 即可） |
| 2026-08-12 | 合併 `報告守則.md` 與 `排程prompt_本機版.MD` 為單一文件（後者已刪除）；補上 Windows 本機指令對照；新增第 16 節裁決表；本檔改為唯一權威來源，不再與 skill `tw-stock-8` 同步 |
| 2026-08-11 | 建立守則；同步為 Cowork skill `tw-stock-8` |
| 2026-08-11 | 新增雙情境建議（空手／持有）；總覽配色改為「視覺重量＝行動急迫性」；修好兩個 bat |
| 2026-08-11 | 定調資料取得原則：瀏覽器為主、來源精簡、不過度交叉驗證、用完關分頁 |
| 2026-08-10 | 新增財報三表（損益表近 8 季／資產負債表三期／現金流量表含 CFO÷淨利），併入基本面 20% |

---

## 免責

本報告由自動化流程彙整公開資訊產生，僅供研究與教育參考，不構成投資建議。所有評分為依既定公式計算之相對參考值，不代表對個股未來表現之預測。與公司正式公告或主管機關公開資訊有出入時，一律以官方公告為準。投資有風險，過去績效不代表未來表現。
